// Command goxctl-claude 同步团队 AI 协作配置（steering / CLAUDE.md）。
//
// 通常作为 goxctl 的子命令运行：goxctl claude <command>。
package main

import "github.com/chinayin/goxctl-claude/internal/cmd"

func main() {
	cmd.Execute()
}
